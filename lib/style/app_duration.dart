class AppDuration {
  static const Duration fast = Duration(milliseconds: 500);
  static const Duration regular = Duration(seconds: 1);
  static const Duration slow = Duration(seconds: 1, milliseconds: 500);
}
